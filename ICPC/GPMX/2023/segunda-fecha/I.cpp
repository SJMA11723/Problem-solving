#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
            long long N,M,K;
    cin>>N>>M>>K;
    if(N/K>=M) cout<<"Iron fist Ketil"<<endl;
    else{cout<<"King Canute"<<endl;}
    return 0;
}
